# H5Player 错误代码101解决方案

## 问题分析

错误代码101通常表示**网络连接错误**，可能的原因包括：

1. **网络连接问题** - 网络不稳定或断开
2. **目标IP不可达** - 摄像头设备离线或IP地址错误
3. **WebSocket服务未运行** - 目标设备上的WebSocket服务未启动
4. **防火墙阻止** - 防火墙阻止了WebSocket连接
5. **IP地址格式错误** - 传入的IP地址格式不正确

## 已实施的解决方案

### 1. IP地址验证 ✅
```javascript
function isValidIP(ip) {
  const ipRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$/;
  return ipRegex.test(ip);
}
```

### 2. 网络连接状态检查 ✅
```javascript
function checkNetworkConnection() {
  return navigator.onLine;
}
```

### 3. 连接可达性测试 ✅
```javascript
async function testConnection(ip) {
  try {
    const response = await fetch(`http://${ip}`, { 
      method: 'HEAD',
      mode: 'no-cors',
      timeout: 5000
    });
    return true;
  } catch (error) {
    return await pingTest(ip);
  }
}
```

### 4. 改进的错误处理 ✅
- 根据错误代码101提供详细的故障排除建议
- 错误节流机制（5秒内只输出一次相同错误）
- 自动重试机制（最多3次，指数退避）

### 5. 网络状态监听 ✅
```javascript
window.addEventListener('online', () => {
  console.log('H5Player: 网络连接已恢复');
  if (retryCount > 0) {
    retryCount = 0;
    setTimeout(() => playerPlay(), 1000);
  }
});
```

### 6. 连接超时保护 ✅
- 10秒连接超时
- 防止无限等待

## 使用说明

### 检查步骤
1. **验证IP地址格式** - 确保IP地址格式正确
2. **检查网络连接** - 确保设备在线
3. **测试目标可达性** - 在连接前先测试IP是否可达
4. **连接超时保护** - 避免长时间等待

### 错误信息解读
当出现错误101时，控制台会显示详细的故障排除建议：

```
H5Player: 播放器错误 - 网络连接错误，请检查：
1. 网络连接是否正常
2. 目标IP地址是否可达
3. WebSocket服务是否运行
4. 防火墙是否阻止连接
```

### 自动重试机制
- 最多重试3次
- 指数退避延迟（1秒、2秒、4秒）
- 达到最大重试次数后停止，等待手动重试

## 常见问题解决

### 1. IP地址不可达
- 检查摄像头设备是否在线
- 验证IP地址是否正确
- 检查网络连接

### 2. WebSocket连接失败
- 确认目标设备上的WebSocket服务正在运行
- 检查端口是否开放
- 验证防火墙设置

### 3. 网络不稳定
- 检查网络连接质量
- 尝试使用有线连接
- 重启网络设备

## 预防措施

1. **定期检查设备状态** - 确保摄像头设备正常运行
2. **网络监控** - 监控网络连接质量
3. **错误日志** - 记录错误信息以便排查问题
4. **备用连接** - 配置多个备用IP地址

## 技术细节

### 连接流程
1. 验证IP地址格式
2. 检查网络连接状态
3. 测试目标IP可达性
4. 初始化播放器
5. 建立WebSocket连接
6. 开始视频流传输

### 错误处理流程
1. 捕获错误
2. 分析错误类型
3. 提供详细错误信息
4. 决定是否重试
5. 记录错误日志

这个解决方案应该能够显著减少错误101的发生，并提供更好的错误诊断和自动恢复能力。